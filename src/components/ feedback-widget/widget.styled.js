import { styled } from 'styled-components';

export const feedback_title = styled.h1` {
  font-size: 45px;
  font-weight: 700;
}`
export const feedback_buttons = styled.button`
  padding: 5px 10px;
  margin: 5px;
  text-transform: capitalize;
 `
export const feedback = styled.div`
  text-align: center;
  margin-left: auto;
  margin-right: auto;

 `